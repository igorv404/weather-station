spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/weather_station_data
    username: alien
    password: rootroot
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  rabbitmq:
    host: localhost
    port: 5672
    username: alien
    password: rootroot
